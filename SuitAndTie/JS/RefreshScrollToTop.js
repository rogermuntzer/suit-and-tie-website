window.onload = function () { //When the page loads, it triggers the function
    window.scroll({ //It then scrolls to the top left of the page "smoothly"
        top: 0, 
        left: 0, 
        behavior: "smooth"
      });
}; 